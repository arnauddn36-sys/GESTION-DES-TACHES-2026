meta {
  name: Login wrong password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {"email": "alice@example.com", "password": "MauvaisMotDePasse"}
}

tests {
  test("statut 401", function () { expect(res.status).to.be.oneOf([401]); });
}

script:post-response {
  bru.runner.setNextRequest("Protected route without token");
}


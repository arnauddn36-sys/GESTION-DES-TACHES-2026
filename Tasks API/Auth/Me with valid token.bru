meta {
  name: Me with valid token
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenAlice}}
}

tests {
  test("statut 200", function () { expect(res.status).to.be.oneOf([200]); });
}

script:post-response {
  bru.runner.setNextRequest("Create task");
}


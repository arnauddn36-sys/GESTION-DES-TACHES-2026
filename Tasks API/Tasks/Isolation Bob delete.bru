meta {
  name: Isolation Bob delete
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/tasks/{{taskId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenBob}}
}

tests {
  test("statut 403, 404", function () { expect(res.status).to.be.oneOf([403, 404]); });
}

script:post-response {
  bru.runner.setNextRequest("Delete task");
}


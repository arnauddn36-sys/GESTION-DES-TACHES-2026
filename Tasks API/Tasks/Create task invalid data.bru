meta {
  name: Create task invalid data
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/tasks
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenAlice}}
}

body:json {
  {"title": "", "priority": "urgent"}
}

tests {
  test("statut 400", function () { expect(res.status).to.be.oneOf([400]); });
}

script:post-response {
  bru.runner.setNextRequest("Get my tasks");
}


meta {
  name: Delete task
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/tasks/{{taskId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenAlice}}
}

tests {
  test("statut 204", function () { expect(res.status).to.be.oneOf([204]); });
}

script:post-response {
  bru.runner.setNextRequest("Get deleted task");
}


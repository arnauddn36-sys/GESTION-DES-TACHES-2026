meta {
  name: Isolation Bob complete
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/tasks/{{taskId}}/complete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenBob}}
}

tests {
  test("statut 403, 404", function () { expect(res.status).to.be.oneOf([403, 404]); });
}

script:post-response {
  bru.runner.setNextRequest("Isolation Bob delete");
}

